import React from 'react'

type Props = {}

export default function Loading({}: Props) {
  return (
    <div className='w-full h-screen bg-white text-primary text-xl flex justify-center items-center font-bold'>
        loading
        </div>
  )
}